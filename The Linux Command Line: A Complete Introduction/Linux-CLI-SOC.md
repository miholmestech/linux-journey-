
# Linux CLI Mastery: Shotts' Guide + Kali Application (Ch. 1-20)

**Progress**: Reached Ch. 20 (Text Processing) via daily reading. No initial notes; retrospective demos prove retention.

## 1. Book Foundation (Bash-Centric)
- Ch. 1-10: Navigation (`cd`, `ls -la`), file ops (`touch`, `cp -r`)
- Ch. 11-20: Text tools (`grep`, `awk`, `sed`) for log parsing

## 2. Kali zsh Challenge & Fix
**Discovery**: Kali 202x defaults to zsh (Oh-My-Zsh), breaking Shotts' bash examples (e.g., `!!` history fails).

**Screenshot**: [Insert terminal: `echo $SHELL` shows `/bin/zsh`]

**Solutions Applied**:

**Output Screenshot**: [Top 5 failed IPs—mock SOC alert triage]

## 4. Key Takeaway
Shells/distros vary; verified bash compatibility ensures scripting portability across SOC endpoints (Kali, Ubuntu, RHEL).

**Timeline**: 3 weeks self-paced. Next: Scripting (Ch. 21+) + RangeForce Core 4 integration.
